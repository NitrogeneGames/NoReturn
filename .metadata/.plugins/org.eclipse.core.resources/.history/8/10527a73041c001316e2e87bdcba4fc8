package nitrogene.util;

import java.util.Random;

public class Stars {
	private int biggeststarsize, n;
	private int[] starx, stary;
	
	public Stars(int biggeststarsize, int spreadwidth, int spreadheight){
		this.biggeststarsize = biggeststarsize;
    	Random rand = new Random();
    	n = rand.nextInt(10) + 510;
    	starx = new int[n];
    	stary = new int[n];
    	for(int i = 0; i < n; i++) {
    	Random randomx = new Random();
    	starx[i] = randomx.nextInt(spreadwidth - biggeststarsize) + 1;
    	Random randomy = new Random();
    	stary[i] = randomy.nextInt(spreadheight - biggeststarsize) + 1;
	}
}
