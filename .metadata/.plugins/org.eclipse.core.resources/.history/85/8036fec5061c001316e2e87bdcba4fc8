package nitrogene.util;

import java.util.Random;

import org.newdawn.slick.Image;
import org.newdawn.slick.SlickException;

public class Stars {
	private int n;
	private int[] starx, stary;
	private Image twopixelstar;
	
	public Stars(int biggeststarsize, int spreadwidth, int spreadheight) throws SlickException{
    	Random rand = new Random();
    	n = rand.nextInt(10) + 510;
    	starx = new int[n];
    	stary = new int[n];
    	for(int i = 0; i < n; i++) {
    	Random randomx = new Random();
    	starx[i] = randomx.nextInt(spreadwidth - biggeststarsize) + 1;
    	Random randomy = new Random();
    	stary[i] = randomy.nextInt(spreadheight - biggeststarsize) + 1;
    	}
    	
    	twopixelstar = new Image("res/star2.png");
	}
}
