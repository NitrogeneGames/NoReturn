package nitrogene.server;

public class PacketBuilder {
/*things to send/receive
 * client must send input to the server
 * inputs are sent to the server
 * movement is processed on server, laser and all that jazz
 * server send back coordinates of everything in lists
 * essentially, the update() function will occur on the server, the render() function will occur on the client
 * input is detected, sent to server, update() run on server, data sent to clients, render() is called
 */
}
